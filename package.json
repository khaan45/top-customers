{
  "name": "top-customer-vote-backend",
  "version": "1.0.0",
  "description": "Real backend for the Top Customer Vote app: student lookup, WhatsApp/SMS OTP delivery, vote recording. Uses Postgres (Neon) instead of local SQLite so it works on Render's free tier with no persistent disk needed.",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "initdb": "node scripts/init-db.js",
    "import-transactions": "node scripts/import-transactions.js transactions_export.csv",
    "seed-todays-transaction": "node scripts/seed-todays-transaction.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "pg": "^8.12.0"
  }
}
